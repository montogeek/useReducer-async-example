import React from "react";

function ErrorMessage() {
  return (
    <div class="alert alert-danger" role="alert">
      An error as ocurred, please try again later.
    </div>
  );
}

export default ErrorMessage;
